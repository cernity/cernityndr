# Pinned for reproducible public-release builds + SBOM (U8). This deliberately
# narrows the repo-wide :latest preference for THIS release artifact, where
# reproducibility is the concrete reason the preference allows pinning.
kafka-python-ng==2.2.3
lz4==4.4.5
python-snappy==0.7.3
zstandard==0.25.0
redis==8.1.0
prometheus_client==0.26.0
